package Gestores;

import java.util.concurrent.Semaphore;

import App.Parque;

public class GestorParque /* implements Runnable */ {
/*     private final Parque parque;
    private final GestorTiempo gestorTiempo;
    private final Semaphore semaforoGestor = new Semaphore(0);

    private final int id;

    public GestorParque(Parque parque, GestorTiempo gestorTiempo, int id) {
        this.parque = parque;
        this.gestorTiempo = gestorTiempo;
        this.id = id;
    }

    @Override
    public void run() {
        while (true) {
            // se duerme con semaforo
            // abre el parque
            // se duerme con semaforo
            // cierra el parque
            // se duerme con semaforo
            // cierra las atracciones
            // se duerme con semaforo
            // saca a todos del parque

            try {
                semaforoGestor.acquire();
            } catch (InterruptedException e) {
                // TODO Auto-generated catch block
                e.printStackTrace();
            }
        }
    } */

    
}